/**
 * If you change this name, you also needs to change the prefix of the secrets at
 * docker-events-listener-build/listen-docker-events.sh
 *
 * Ex: app-region-dev -> NEW_PREFIX-region-dev
 */
export const SERVICE_NAME = "app";
